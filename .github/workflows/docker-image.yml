name: Docker Image CI

on:
  push:
    branches: [ "ci/**", "docker/**" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: ./build.sh
